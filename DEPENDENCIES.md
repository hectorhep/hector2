Minimal requirements for `Hector`:

- a standard compiler suite, such as `gcc` (`gcc-c++`) or `clang`
- `CLHEP`, with development headers

For `pyHector`:

- `boost-python`, `boost-python-devel`
- `python`, `python-devel`
